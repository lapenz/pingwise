<div class="min-h-screen bg-gray-50 py-8">
  <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="mb-8">
      <h1 class="text-3xl font-bold text-gray-900">Edit Endpoint</h1>
      <p class="mt-2 text-gray-600">Update monitoring configuration for <%= @endpoint.name %></p>
    </div>

    <div class="bg-white rounded-lg shadow p-6">
      <%= render 'form', endpoint: @endpoint %>
    </div>
  </div>
</div>
